const { addMember } = require('./addMember');
const { existMember } = require('./existMember');
const { removeMember } = require('./removeMember');

module.exports = {
  addMember,
  existMember,
  removeMember,
};

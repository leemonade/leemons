const {
  getLocation,
  addLocation,
  updateLocation,
  existLocation,
} = require('../src/services/dataset-location');

module.exports = {
  getLocation,
  addLocation,
  updateLocation,
  existLocation,
};

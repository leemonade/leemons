import React from 'react';
import { Box } from '@bubbles-ui/components';

function UserDetailWidget({ ...props }) {
  console.log(props);
  return <Box>Gatitos familia</Box>;
}

export default UserDetailWidget;

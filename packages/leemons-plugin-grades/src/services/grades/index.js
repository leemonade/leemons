const { addGrade } = require('./addGrade');
const { listGrades } = require('./listGrades');
const { gradeByIds } = require('./gradeByIds');
const { updateGrade } = require('./updateGrade');

module.exports = {
  addGrade,
  listGrades,
  gradeByIds,
  updateGrade,
};

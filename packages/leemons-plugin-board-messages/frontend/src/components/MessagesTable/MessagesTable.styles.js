import { createStyles } from '@bubbles-ui/components';

// eslint-disable-next-line import/prefer-default-export
export const MessagesTableStyles = createStyles(() => ({
  root: { display: 'flex', flexDirection: 'column', gap: 16 },
}));

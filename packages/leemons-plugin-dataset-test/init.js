const initDataset = require('./src/dataset');
const initItemPermission = require('./src/item-permission');
const initMenuBuilder = require('./src/menu-builder');

async function init() {
  // await initDataset();
  // await initItemPermission();
  // await initMenuBuilder();
}

module.exports = init;

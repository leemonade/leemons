const { exist } = require('./exist');
const { addMany } = require('./addMany');
const { add } = require('./add');
const { init } = require('./init');

module.exports = {
  init,

  exist,

  add,
  addMany,
};

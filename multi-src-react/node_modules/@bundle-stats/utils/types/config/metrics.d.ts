export const METRIC_TYPE_FILE_SIZE: "METRIC_TYPE_FILE_SIZE";
export const METRIC_TYPE_DURATION: "METRIC_TYPE_DURATION";
export const METRIC_TYPE_NUMERIC: "METRIC_TYPE_NUMERIC";
export const METRIC_TYPE_SCORE: "METRIC_TYPE_SCORE";
export const METRIC_TYPE_PERCENTAGE: "METRIC_TYPE_PERCENTAGE";
export namespace METRIC_TYPES {
    namespace METRIC_TYPE_NUMERIC {
        export const biggerIsBetter: boolean;
        export { formatNumber as formatter };
    }
    namespace METRIC_TYPE_SCORE {
        const biggerIsBetter_1: boolean;
        export { biggerIsBetter_1 as biggerIsBetter };
        export { formatNumber as formatter };
    }
    namespace METRIC_TYPE_DURATION {
        const biggerIsBetter_2: boolean;
        export { biggerIsBetter_2 as biggerIsBetter };
        export { formatDuration as formatter };
    }
    namespace METRIC_TYPE_FILE_SIZE {
        const biggerIsBetter_3: boolean;
        export { biggerIsBetter_3 as biggerIsBetter };
        export { formatFileSize as formatter };
    }
    namespace METRIC_TYPE_PERCENTAGE {
        const biggerIsBetter_4: boolean;
        export { biggerIsBetter_4 as biggerIsBetter };
        export { formatPercentage as formatter };
    }
}
import { formatNumber } from "../utils/format";
import { formatDuration } from "../utils/format";
import { formatFileSize } from "../utils/format";
import { formatPercentage } from "../utils/format";

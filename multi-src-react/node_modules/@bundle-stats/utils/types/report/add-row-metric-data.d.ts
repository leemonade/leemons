import * as types from './types';
export declare const getAddRowMetricData: (metricType?: string | undefined) => (row: types.ReportRow) => {
    biggerIsBetter: any;
    label: any;
    changed: boolean;
    runs: any[];
    key: string;
};

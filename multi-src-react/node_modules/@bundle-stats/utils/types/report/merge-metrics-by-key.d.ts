import * as types from './types';
declare type RunMetrics = Record<string, types.MetricValue>;
export declare const mergeMetricsByKey: (runs: Array<RunMetrics>, rowTransformers?: ((row: types.ReportRow) => types.ReportRow)[] | undefined) => Array<types.ReportRow>;
export {};

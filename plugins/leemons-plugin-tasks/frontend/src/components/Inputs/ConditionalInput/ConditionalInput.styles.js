import { createStyles } from '@bubbles-ui/components';

const ConditionalInputStyles = createStyles((theme) => ({
  root: {
    paddingLeft: 22,
    paddingTop: 0,
    paddingBottom: 0,
    height: 'auto',
    display: 'flex',
    flexDirection: 'column',
  },
}));

export { ConditionalInputStyles };

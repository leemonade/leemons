import { createStyles } from '@bubbles-ui/components';

const ClassroomPickerStyles = createStyles((theme) => ({
  selectAllContainer: {
    display: 'flex',
  },
  selectAllText: {
    // marginLeft: theme.spacing(1),
  },
  selectAllIcon: {
    // marginRight: theme.spacing(1),
  },
}));

export { ClassroomPickerStyles };

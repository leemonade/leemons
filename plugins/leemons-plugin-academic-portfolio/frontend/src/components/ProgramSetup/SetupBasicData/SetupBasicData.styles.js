import { createStyles } from '@bubbles-ui/components';

export const SetupBasicDataStyles = createStyles((theme, {}) => {
  return {
    root: {},
    inputHeader: {
      width: '40%',
    },
  };
});

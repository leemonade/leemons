import { createStyles } from '@bubbles-ui/components';

const EnrollmentTabStyles = createStyles((theme) => ({
  root: {},
  courseData: {
    // gap: 25,
  },
  mainTeacher: {
    width: '25%',
  },
  inlineInputs: {
    width: '50%',
  },
}));

export { EnrollmentTabStyles };

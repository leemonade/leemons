import { createStyles } from '@bubbles-ui/components';

const useBlockPageStyles = createStyles((theme) => ({
  root: {},
  abbreviationInput: {
    width: 155,
    minHeight: 88,
  },
  nameInput: {
    width: 276,
    minHeight: 88,
  },
}));

export { useBlockPageStyles };

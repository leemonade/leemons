import propTypes from 'prop-types';

export const EVALUATION_STATE_DISPLAY_PROP_TYPES = {
  assignation: propTypes.object,
};

export const EVALUATION_STATE_DISPLAY_DEFAULT_PROPS = {
  assignation: null,
};

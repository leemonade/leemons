import { createStyles } from '@bubbles-ui/components';

const OpenResponseStyles = createStyles((theme) => ({
  root: {},
  numberInput: {
    width: 136,
  },
  inputBox: {
    paddingInline: 16,
  },
}));

export default OpenResponseStyles;

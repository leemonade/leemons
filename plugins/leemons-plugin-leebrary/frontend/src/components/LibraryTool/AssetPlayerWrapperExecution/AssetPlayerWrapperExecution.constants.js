import PropTypes from 'prop-types';

export const ASSET_PLAYER_WRAPPER_EXECUTION_PROPTYPES = {
  asset: PropTypes.object,
};

export const ASSET_PLAYER_WRAPPER_EXECUTION_DEFAULT_PROPS = {
  asset: null,
};

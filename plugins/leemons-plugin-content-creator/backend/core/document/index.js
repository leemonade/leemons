const getDocument = require('./getDocument');
const saveDocument = require('./saveDocument');
const shareDocument = require('./shareDocument');
const deleteDocument = require('./deleteDocument');
const assignDocument = require('./assignDocument');
const duplicateDocument = require('./duplicateDocument');

module.exports = {
  getDocument,
  saveDocument,
  shareDocument,
  assignDocument,
  deleteDocument,
  duplicateDocument,
};

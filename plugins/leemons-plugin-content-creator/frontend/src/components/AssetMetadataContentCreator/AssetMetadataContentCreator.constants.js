import propTypes from 'prop-types';

export const ASSET_METADATA_CONTENT_CREATOR_PROP_TYPES = {
  metadata: propTypes.object,
};

export const ASSET_METADATA_CONTENT_CREATOR_DEFAULT_PROPS = {
  metadata: null,
};

import PropTypes from 'prop-types';

export const EVALUATIONCARD_BODY_DEFAULT_PROPS = {
  instance: {},
  localizations: {},
};

export const EVALUATIONCARD_BODY_PROP_TYPES = {
  instance: PropTypes.object,
  localizations: PropTypes.object,
};

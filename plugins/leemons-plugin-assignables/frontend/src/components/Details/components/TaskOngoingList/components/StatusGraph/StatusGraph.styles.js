import { createStyles } from '@bubbles-ui/components';

export const useStatusGraphStyles = createStyles(() => ({
  root: {
    width: '100%',
    height: 600,
    marginBottom: -250,
  },
}));

export default useStatusGraphStyles;
